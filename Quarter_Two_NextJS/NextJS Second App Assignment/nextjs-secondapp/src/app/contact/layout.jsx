import React from 'react'

export default function layout({ children }) {
    return (
      <html lang="en">
        <body>
        <h2>HEADER FROM CONTACT</h2>
          {children}</body>
      </html>
    )
  }